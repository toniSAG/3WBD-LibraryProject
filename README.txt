L'application a été crée avec le serveur de développement
Javascript vueJS

une fois le fichier installé aller dans le répertoire racine
    cd library-project

    installer node module:
    npm i

    installer les packages nécessaires:
    npm i path
    npm i axios

    pour lancer l'application:
    npm run dev

    L'application se lance sur le port 5173